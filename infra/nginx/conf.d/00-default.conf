# Catch-all for unknown subdomains. Per-project routes are generated by the
# worker as 10-<project>.conf files next to this one; never edit those by hand.
server {
    listen 80 default_server;
    server_name _;
    return 404 "no such deployment\n";
}
